import { OverlayLoadingDirective } from './overlay-loading.directive';

describe('OverlayLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
